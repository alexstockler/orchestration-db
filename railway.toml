[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m instrdb.migrate && uvicorn api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/composers"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
